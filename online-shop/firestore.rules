

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /{document=**} {
      allow read, write: if true
    }

    // match /Users/{userId} {
    //   allow write: if request.auth != null && request.auth.uid == userId && "cart" in request.resource.data;
    // }

    // match /Products/{productId}/{product=**} {
    //   allow read: if true
    // }

    // match /Categories/{categoryId}/{category=**} {
    //   allow read: if true
    // }
    
  }
}