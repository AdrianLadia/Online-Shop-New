class conditionalStatements {
    
    accountStatementGetOrderFromReference(referencenumber,order) {
        orders.map((order) => {
            if (order.reference === reference) { 
                return order 
             }
        });
    }
}