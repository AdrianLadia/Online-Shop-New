rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    //ADMIN RULES
    match /{document=**} {
      allow create,read,update,delete:
      	if true
    }
    
  }
}
